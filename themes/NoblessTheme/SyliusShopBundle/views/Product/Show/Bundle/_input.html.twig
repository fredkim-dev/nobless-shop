<input type="radio" id="{{ form_row_variables.id }}" name="{{ form_row_variables.full_name }}" required="required" class="form-check-input" value="{{ form_row_variables.value }}" {% if selectedInput == form_row_variables.id %}checked{% endif%}>
