{% extends 'knp_menu.html.twig' %}

{% block root %}
    {% for item in item.children %}
        <a class="account-link nav-item nav-link" href="{{ item.uri }}">{{ item.label|trans }}</a>
    {% endfor %}
    <a class="account-link nav-item nav-link" href="{{ path('sylius_shop_logout') }}">{{ 'sylius.ui.logout'|trans }}</a>
{% endblock %}