{{ form_start(form, {'action': path('sylius_shop_to_register'), 'method': 'POST', 'attr': {'class': 'register-form col-12'}}) }}
    <div class="form-group">
        {{ form_widget(form.username) }}
    </div>
    <button type="submit" class="col-12 btn btn-primary">{{ 'sylius.ui.create_an_account'|trans }}</button>
{{ form_end(form, {'render_rest': false}) }}
