{% if form.vars.choices|length == 1 %}
    {{ form_label(form) }}
    {{ form_widget(form, {'value': form.vars.choices[0].value}) }}
    {{ form_errors(form) }}
{% else %}
    {{ form_label(form) }}
    {{ form_widget(form) }}
    {{ form_errors(form) }}
{% endif %}
